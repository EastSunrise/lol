<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="wsg.lol.dao.mybatis.mapper.system.ConfigMapper">
    <select id="getConfigValue" resultType="java.lang.String">
        select value
        from lol.t_config
        where name = #{name}
          and region = #{region}
    </select>

    <update id="updateConfigValue">
        update lol.t_config
        set value = #{value}
        where name = #{name}
          and region = #{region}
    </update>
</mapper>